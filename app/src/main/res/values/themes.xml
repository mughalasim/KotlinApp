<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.KotlinApp" parent="Theme.MaterialComponents.DayNight.DarkActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/label_text</item>
        <item name="colorPrimaryVariant">@color/list_text</item>
        <item name="colorOnPrimary">@color/list_text</item>
        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/list_text</item>
        <item name="colorSecondaryVariant">@color/label_text</item>
        <item name="colorOnSecondary">@color/label_text</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor" tools:targetApi="l">?attr/colorPrimaryVariant</item>
        <!-- Customize your theme here. -->
        <item name="android:windowBackground">@color/background</item>
    </style>
</resources>